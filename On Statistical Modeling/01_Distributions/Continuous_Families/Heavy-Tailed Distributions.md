---
tags: [statistics, distributions, parametric-families, continuous]
aliases: [Cauchy Distribution, Laplace Distribution, Fat Tails]
created: 2026-03-04
---
# Heavy-Tailed Distributions

## Definition
While the [[Normal Distribution]] assumes extreme outliers are exceptionally rare (due to exponential density drop-off), real-world data generating processes often allocate significant probability to extreme events. These are mathematically classified as **Heavy-Tailed Distributions**.

## Key Examples
* **The Cauchy Distribution:** A symmetric distribution that visually resembles a normal curve, but its tails are so thick that its theoretical mean and variance are strictly undefined (infinite). It catastrophically destroys standard limit laws.
* **The Laplace Distribution:** Symmetrical, but features a much sharper peak and "fatter" exponential tails. 

## Statistical Consequences
When empirical data is generated by a heavy-tailed distribution, standard parametric models (like the [[Gaussian Linear Model]]) will fail because their [[Ordinary Least Squares (OLS)]] estimators are overly sensitive to the massive outliers these distributions routinely produce. 

To illustrate, follow the below example - 
## The Cauchy Paradox: Why the Mean Does Not Exist
The standard Cauchy distribution is defined by the PDF: $f(x) = \frac{1}{\pi(1+x^2)}$.

To find the mean ($E[X]$), we must evaluate:
$$E[X] = \int_{-\infty}^{\infty} \frac{x}{\pi(1+x^2)} dx$$

### Mathematical Proof of Failure
1. The integral for the mean is only well-defined if it converges absolutely: $\int |x| f(x) dx < \infty$.
2. As $x \to \infty$, the function $x f(x) \approx \frac{x}{x^2} = \frac{1}{x}$.
3. The integral of $\frac{1}{x}$ is $\ln(x)$, which diverges to $\infty$.
4. Because the positive half is $+\infty$ and the negative half is $-\infty$, the sum is **indeterminate**. 

**Conclusion:** The Cauchy distribution has **no mean** and **infinite variance**.



## Collapse of the Central Limit Theorem (CLT)
The [[Central Limit Theorem]] relies on the assumption of a finite variance ($\sigma^2 < \infty$). 

### The Characteristic Function Proof
For a Cauchy variable, the characteristic function is $\phi(t) = e^{-|t|}$.
If we take the average of $n$ independent Cauchy variables $\bar{X} = \frac{\sum X_i}{n}$:
$$\phi_{\bar{X}}(t) = \left[ e^{-|t/n|} \right]^n = e^{-n \frac{|t|}{n}} = e^{-|t|}$$

**The "Infinite Noise" Result:**
The distribution of the sample mean ($\bar{X}$) is **identical** to the distribution of a single observation ($X_1$). Averaging Cauchy data provides **zero** reduction in noise, regardless of how many samples you take. This is why the CLT "collapses"—the bell curve never forms.

---
**Connections:**
* Violates the requirements for [[Ordinary Least Squares (OLS)]] and most parametric tests.

---
**Connections:**
* If the variance is infinite (like in the Cauchy distribution), the [[Central Limit Theorem]] collapses entirely.