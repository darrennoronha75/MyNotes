---
tags: [statistics, foundations, theory]
aliases: [DGP, Underlying Distribution]
created: 2026-03-04
---
# Data Generating Process

## Definition
The Data Generating Process (DGP) is the true, underlying, and unobservable probabilistic engine that produces the empirical data we collect in reality. Mathematically, it is the true [[Probability Distribution]] governing a population.

## Intuition
Statisticians are trapped behind an "observational wall." We *always* only see the empirical data generated by the distribution (the outputs), but we never see the true mathematical distribution itself. Knowing the exact DGP would allow us to derive every possible information point with perfect certainty. Because it is hidden, our goal is to **estimate** it.

## Estimation Strategies
* **Parametric Estimation:** Imposing a pre-defined mathematical structure (a Parametric Family) onto the data to drastically simplify the problem. Instead of guessing the entire shape of reality, we assume the shape and only estimate a few constants (parameters, $\theta$). If the assumption is wrong, the model is invalid.
* **Non-parametric Estimation:** Making no structural assumptions about the DGP. We estimate the shape visually or empirically (e.g., using Empirical Distribution Functions or histograms) based entirely on the relative frequency of the observed data.

---
**Connections:**
* The leap from raw data to structural assumptions leads directly to the use of continuous families like the [[Normal Distribution]].
* The methods used to approximate the DGP parameters are covered in [[Maximum Likelihood Estimation (MLE)]] and [[Ordinary Least Squares (OLS)]].