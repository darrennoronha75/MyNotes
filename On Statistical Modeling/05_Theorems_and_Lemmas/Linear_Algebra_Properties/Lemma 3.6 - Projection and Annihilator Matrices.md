---
tags: [statistics, theorems, linear-algebra, linear-regression]
aliases: [Lemma 3.6, Annihilator Matrix, Projection Matrix]
created: 2026-03-10
---
# Lemma 3.6 - Projection and Annihilator Matrices

## Motivation
To evaluate any linear model, we must mathematically separate the "signal" from the "noise." This lemma provides the exact linear algebra machinery to do so. It guarantees that the predictions ($\hat{Y}$) exist strictly within the model's geometric space, and that the resulting residuals ($\hat{\varepsilon}$) are perfectly orthogonal (perpendicular) to those predictions. 


## The Theorem
Let $\mathbb{L}$ be the linear subspace generated by the columns of the design matrix $A$, i.e., $\mathbb{L} = \{Av : v \in \mathbb{R}^{k+1}\}$. 

**a) The Projection Matrix ($\Pi_{\mathbb{L}}$)**
The matrix $A(A^T A)^{-1}A^T$ is the exact orthogonal projection $\Pi_{\mathbb{L}}$ onto $\mathbb{L}$[. 
In the context of the linear model, this means the fitted values and residuals can be expressed entirely via linear transformations of the response vector $Y$:
* $\hat{Y} = A(A^T A)^{-1}A^T Y = \Pi_{\mathbb{L}}Y$
* $\hat{\varepsilon} = (I_n - A(A^T A)^{-1}A^T)Y = (I_n - \Pi_{\mathbb{L}})Y$

**b) The Annihilator Matrix**
The matrix $(I_n - A(A^T A)^{-1}A^T)$, often called the Annihilator Matrix, is symmetric and idempotent:
$$(I_n - A(A^T A)^{-1}A^T)^2 = (I_n - A(A^T A)^{-1}A^T)$$

## Proof

**Part A (Orthogonality):**
For any $z \in \mathbb{R}^n$, we have $A(A^T A)^{-1}A^T z = Av$ where $v = (A^T A)^{-1}A^T z \in \mathbb{R}^{k+1}$, meaning the projection strictly lives in $\mathbb{L}$.
To prove it is orthogonal, we show the residual vector is perpendicular to the subspace. We multiply $A^T$ by the residual:
$$A^T(z - A(A^T A)^{-1}A^T z) = A^T z - A^T A(A^T A)^{-1}A^T z = A^T z - A^T z = \mathbf{0}$$
Thus, $\langle Av, z - A(A^T A)^{-1}A^T z \rangle = 0$ for all $v \in \mathbb{R}^{k+1}$, proving $z - \Pi_{\mathbb{L}}z \perp \mathbb{L}$.

**Part B (Symmetry and Idempotence):**
Because $A^T A$ and its inverse are symmetric, $(A(A^T A)^{-1}A^T)^T = A(A^T A)^{-1}A^T$. Therefore, both the projection matrix and the annihilator matrix $I_n - \Pi_{\mathbb{L}}$ are symmetric.
Using the property that projections are idempotent ($\Pi_{\mathbb{L}}^2 = \Pi_{\mathbb{L}}$):
$$(I_n - \Pi_{\mathbb{L}})^2 = I_n^2 - I_n\Pi_{\mathbb{L}} - \Pi_{\mathbb{L}}I_n + \Pi_{\mathbb{L}}^2 = I_n - 2\Pi_{\mathbb{L}} + \Pi_{\mathbb{L}} = I_n - \Pi_{\mathbb{L}}$$ [cite: 437]

---
**Connections:**
* Links directly to the definitions of [[Hat Matrix]] and [[Orthogonal Projection Matrix]].
* The symmetry and idempotence proven here are critical for calculating $E[\|\hat{\varepsilon}\|^2]$ in [[Theorem 3.7 - Unbiasedness and Covariance of OLS]].