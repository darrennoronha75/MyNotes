In order to develop statistical tests, we need stronger assumptions on the distribution of the random error terms ε1,...,εn. From now on we assume that ε1,...,εn are inde pendent and are all normally distributed with expectation zero and the same unknown standard deviation σ ∈ (0,∞). This is equivalent to ε ∼ Normal(0,σ2In). We refer to the corresponding linear model as Gaussian linear model. As before we assume that ATA is invertible


